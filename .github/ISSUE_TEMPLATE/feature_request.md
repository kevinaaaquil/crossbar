---
name: Feature request
about: Propose a capability crossbar does not have
labels: enhancement
---

**The decision you cannot currently make**

What question are you trying to answer? crossbar exists to support a decision;
features are easiest to judge against one.

**What you want it to do**

**What you do today instead**

**Which layer you think it belongs in**

`tasks` / `mcpclient` / `env` / `providers` / `harness` / `scoring` / `stats` /
`runner` / `analysis` / `report` / `tui` — see
[docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md). A guess is fine.

**Are you offering to build it?**
